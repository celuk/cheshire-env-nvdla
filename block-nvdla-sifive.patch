diff --git a/hw b/hw
--- a/hw
+++ b/hw
@@ -1 +1 @@
-Subproject commit 7cf6ad5a6e75f01aac1b0041288612e63a5999ac
+Subproject commit 7cf6ad5a6e75f01aac1b0041288612e63a5999ac-dirty
diff --git a/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
index 34226ea..4e10af9 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
@@ -485,6 +485,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [10:0] array [0:127];
+    reg [10:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -557,6 +574,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -574,6 +593,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [10:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [10:0] array [0:127];
+    reg [10:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +689,8 @@ output [10:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -945,6 +983,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [10:0] array [0:127];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -952,6 +1007,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
index 4998aff..49413ae 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
@@ -485,6 +485,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 6;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [5:0] array [0:127];
+    reg [5:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -557,6 +574,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -574,6 +593,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [5:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 6;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [5:0] array [0:127];
+    reg [5:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +689,8 @@ output [5:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -945,6 +983,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 6;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [5:0] array [0:127];
+    reg [5:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -952,6 +1007,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v
index c7c7631..9bfe2d9 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X128_GL_M1_E2.v
@@ -335,6 +335,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [127:0] array [0:15];
+    reg [127:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -407,6 +424,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -424,6 +443,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [127:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [127:0] array [0:15];
+    reg [127:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +539,8 @@ output [127:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -783,6 +821,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [127:0] array [0:15];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -790,6 +845,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v
index b404142..d91aba0 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X14_GL_M1_E2.v
@@ -335,6 +335,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [13:0] array [0:15];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -407,6 +424,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -424,6 +443,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [13:0] array [0:15];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +539,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -783,6 +821,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [13:0] array [0:15];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -790,6 +845,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v
index 650bac2..0f1932d 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X16_GL_M1_E2.v
@@ -335,6 +335,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [15:0] array [0:15];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -407,6 +424,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -424,6 +443,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [15:0] array [0:15];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +539,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -783,6 +821,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [15:0] array [0:15];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -790,6 +845,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
index 1c8f413..bce6815 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
@@ -335,6 +335,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [255:0] array [0:15];
+    reg [255:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -407,6 +424,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -424,6 +443,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [255:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [255:0] array [0:15];
+    reg [255:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +539,8 @@ output [255:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -783,6 +821,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [255:0] array [0:15];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -790,6 +845,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
index 08605dd..ad1d344 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
@@ -340,6 +340,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [271:0] array [0:15];
+    reg [271:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -412,6 +429,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -429,6 +448,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [271:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [271:0] array [0:15];
+    reg [271:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +544,8 @@ output [271:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -788,6 +826,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [271:0] array [0:15];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -795,6 +850,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
index 5548288..d778134 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
@@ -340,6 +340,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [63:0] array [0:15];
+    reg [63:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -412,6 +429,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -429,6 +448,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [63:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [63:0] array [0:15];
+    reg [63:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +544,8 @@ output [63:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -788,6 +826,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [63:0] array [0:15];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -795,6 +850,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v
index c700898..aeb7952 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_16X66_GL_M1_E2.v
@@ -335,6 +335,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [65:0] array [0:15];
+    reg [65:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -407,6 +424,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -424,6 +443,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [65:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [65:0] array [0:15];
+    reg [65:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -503,6 +539,8 @@ output [65:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -783,6 +821,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [65:0] array [0:15];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -790,6 +845,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v
index de73dcc..9227bce 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X16_GL_M1_E2.v
@@ -347,6 +347,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [15:0] array [0:19];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -419,6 +436,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -436,6 +455,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [15:0] array [0:19];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -515,6 +551,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -795,6 +833,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [15:0] array [0:19];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -802,6 +857,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
index f7d8b75..5ad2416 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
@@ -352,6 +352,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [287:0] array [0:19];
+    reg [287:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -424,6 +441,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -441,6 +460,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [287:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [287:0] array [0:19];
+    reg [287:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +556,8 @@ output [287:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -800,6 +838,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [287:0] array [0:19];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -807,6 +862,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v
index 71bc39f..c5b7122 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X32_GL_M1_E2.v
@@ -347,6 +347,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [31:0] array [0:19];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -419,6 +436,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -436,6 +455,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [31:0] array [0:19];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -515,6 +551,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -795,6 +833,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [31:0] array [0:19];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -802,6 +857,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
index ddbcbaf..6abd6d8 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
@@ -352,6 +352,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [79:0] array [0:19];
+    reg [79:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -424,6 +441,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -441,6 +460,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [79:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [79:0] array [0:19];
+    reg [79:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +556,8 @@ output [79:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -800,6 +838,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [79:0] array [0:19];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -807,6 +862,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v
index c392012..1863809 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_20X8_GL_M1_E2.v
@@ -347,6 +347,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [7:0] array [0:19];
+    reg [7:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -419,6 +436,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -436,6 +455,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [7:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [7:0] array [0:19];
+    reg [7:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -515,6 +551,8 @@ output [7:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -795,6 +833,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [7:0] array [0:19];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -802,6 +857,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
index bc8af2e..4eed766 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
@@ -613,6 +613,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 4;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [3:0] array [0:255];
+    reg [3:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -685,6 +702,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -702,6 +721,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [3:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 4;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [3:0] array [0:255];
+    reg [3:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +817,8 @@ output [3:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -1073,6 +1111,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 4;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [3:0] array [0:255];
+    reg [3:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1080,6 +1135,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
index 0beca22..e9b3188 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
@@ -613,6 +613,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 7;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [6:0] array [0:255];
+    reg [6:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -685,6 +702,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -702,6 +721,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [6:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 7;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [6:0] array [0:255];
+    reg [6:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +817,8 @@ output [6:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -1073,6 +1111,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 7;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [6:0] array [0:255];
+    reg [6:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1080,6 +1135,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
index cd77293..a452dc6 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
@@ -613,6 +613,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [7:0] array [0:255];
+    reg [7:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -685,6 +702,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -702,6 +721,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [7:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [7:0] array [0:255];
+    reg [7:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +817,8 @@ output [7:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -1073,6 +1111,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [7:0] array [0:255];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1080,6 +1135,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
index 42a3813..9600aeb 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
@@ -356,6 +356,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [15:0] array [0:31];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -428,6 +445,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -445,6 +464,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [15:0] array [0:31];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +560,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -804,6 +842,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [15:0] array [0:31];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -811,6 +866,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
index 1969fbc..b3bc03a 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
@@ -356,6 +356,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [31:0] array [0:31];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -428,6 +445,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -445,6 +464,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [31:0] array [0:31];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +560,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -804,6 +842,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [31:0] array [0:31];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -811,6 +866,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v
index 0b749ec..1cf7af5 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_32X64_GL_M1_E2.v
@@ -351,6 +351,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [63:0] array [0:31];
+    reg [63:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -423,6 +440,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -440,6 +459,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [63:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [63:0] array [0:31];
+    reg [63:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -519,6 +555,8 @@ output [63:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -799,6 +837,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [63:0] array [0:31];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -806,6 +861,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
index 2d611ef..d26d140 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
@@ -387,6 +387,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 22;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [21:0] array [0:59];
+    reg [21:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -459,6 +476,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -476,6 +495,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [21:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 22;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [21:0] array [0:59];
+    reg [21:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -555,6 +591,8 @@ output [21:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -835,6 +873,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 22;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [21:0] array [0:59];
+    reg [21:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -842,6 +897,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
index b54f48b..b81feb0 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
@@ -421,6 +421,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 10;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [9:0] array [0:63];
+    reg [9:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -493,6 +510,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -510,6 +529,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [9:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 10;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [9:0] array [0:63];
+    reg [9:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +625,8 @@ output [9:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -881,6 +919,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 10;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [9:0] array [0:63];
+    reg [9:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -888,6 +943,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v
index 17cdbb0..d74ed00 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X14_GL_M2_E2.v
@@ -416,6 +416,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [13:0] array [0:63];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -488,6 +505,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -505,6 +524,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [13:0] array [0:63];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -584,6 +620,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -876,6 +914,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [13:0] array [0:63];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -883,6 +938,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v
index 4073c77..8bb07c9 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X16_GL_M2_E2.v
@@ -416,6 +416,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [15:0] array [0:63];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -488,6 +505,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -505,6 +524,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [15:0] array [0:63];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -584,6 +620,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -876,6 +914,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [15:0] array [0:63];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -883,6 +938,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
index e85c431..24bc639 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
@@ -421,6 +421,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [17:0] array [0:63];
+    reg [17:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -493,6 +510,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -510,6 +529,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [17:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [17:0] array [0:63];
+    reg [17:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +625,8 @@ output [17:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -881,6 +919,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [17:0] array [0:63];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -888,6 +943,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v
index c1b4ec2..621f227 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_64X32_GL_M1_E2.v
@@ -383,6 +383,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [31:0] array [0:63];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -455,6 +472,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -472,6 +491,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [31:0] array [0:63];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -551,6 +587,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -831,6 +869,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [31:0] array [0:63];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -838,6 +893,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
index 3557d03..bc2a081 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
@@ -440,6 +440,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [13:0] array [0:79];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -512,6 +529,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -529,6 +548,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [13:0] array [0:79];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -608,6 +644,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -900,6 +938,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [13:0] array [0:79];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -907,6 +962,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
index 6cb9768..304a929 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
@@ -445,6 +445,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 15;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [14:0] array [0:79];
+    reg [14:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -517,6 +534,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -534,6 +553,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [14:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 15;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [14:0] array [0:79];
+    reg [14:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +649,8 @@ output [14:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -905,6 +943,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 15;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [14:0] array [0:79];
+    reg [14:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -912,6 +967,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v
index 8458e8e..0b3744b 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X18_GL_M1_E2.v
@@ -407,6 +407,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [17:0] array [0:79];
+    reg [17:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -479,6 +496,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -496,6 +515,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [17:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [17:0] array [0:79];
+    reg [17:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -575,6 +611,8 @@ output [17:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -855,6 +893,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [17:0] array [0:79];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -862,6 +917,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v b/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
index 375f1d5..9cf3dfc 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
@@ -440,6 +440,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 9;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [8:0] array [0:79];
+    reg [8:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -512,6 +529,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -529,6 +548,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [8:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 9;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [8:0] array [0:79];
+    reg [8:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -608,6 +644,8 @@ output [8:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -900,6 +938,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 9;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [8:0] array [0:79];
+    reg [8:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -907,6 +962,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v
index b1b284a..caaaf6a 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_8X130_GL_M1_E2.v
@@ -327,6 +327,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [129:0] array [0:7];
+    reg [129:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -399,6 +416,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -416,6 +435,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [129:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [129:0] array [0:7];
+    reg [129:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +531,8 @@ output [129:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -775,6 +813,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [129:0] array [0:7];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -782,6 +837,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v
index f21bedf..9043fe8 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_8X258_GL_M1_E2.v
@@ -327,6 +327,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 258;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [257:0] array [0:7];
+    reg [257:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -399,6 +416,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -416,6 +435,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [257:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 258;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [257:0] array [0:7];
+    reg [257:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +531,8 @@ output [257:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -775,6 +813,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 258;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [257:0] array [0:7];
+    reg [257:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -782,6 +837,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v b/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
index 58cbe53..e620851 100644
--- a/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
+++ b/vsrc/large/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
@@ -327,6 +327,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [65:0] array [0:7];
+    reg [65:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -399,6 +416,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -416,6 +435,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [65:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [65:0] array [0:7];
+    reg [65:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +531,8 @@ output [65:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -775,6 +813,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [65:0] array [0:7];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -782,6 +837,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v
index 5b29f5a..8033b74 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X128_GL_M1_D2.v
@@ -712,6 +712,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [127:0] array [0:127];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -772,6 +789,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -962,6 +981,23 @@ endtask
 // Ramgen function for reading the arrays
 function [127:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [127:0] array [0:127];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [127:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -970,6 +1006,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1217,6 +1255,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [127:0] array [0:127];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1224,6 +1279,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
index 3cdd88c..2663679 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
@@ -755,6 +755,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [17:0] array [0:127];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -818,6 +835,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1025,6 +1044,23 @@ endtask
 // Ramgen function for reading the arrays
 function [17:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [17:0] array [0:127];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [17:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1036,6 +1072,8 @@ reg [17:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1283,6 +1321,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [17:0] array [0:127];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1290,6 +1345,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v
index 83730e7..000b71c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X224_GL_M1_D2.v
@@ -712,6 +712,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [223:0] array [0:127];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -772,6 +789,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -962,6 +981,23 @@ endtask
 // Ramgen function for reading the arrays
 function [223:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [223:0] array [0:127];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [223:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -970,6 +1006,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1217,6 +1255,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [223:0] array [0:127];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1224,6 +1279,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
index 162e3b8..67a6ae8 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
@@ -718,6 +718,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [255:0] array [0:127];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -778,6 +795,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -968,6 +987,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [255:0] array [0:127];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -976,6 +1012,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1223,6 +1261,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [255:0] array [0:127];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1230,6 +1285,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v
index 1a6e95f..e61a535 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X258_GL_M1_D2.v
@@ -712,6 +712,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 258;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [257:0] array [0:127];
+    reg [257:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -772,6 +789,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -962,6 +981,23 @@ endtask
 // Ramgen function for reading the arrays
 function [257:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 258;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [257:0] array [0:127];
+    reg [257:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [257:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -970,6 +1006,8 @@ reg [257:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1217,6 +1255,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 258;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [257:0] array [0:127];
+    reg [257:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1224,6 +1279,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v
index 47eac09..9955e79 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X288_GL_M1_D2.v
@@ -712,6 +712,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [287:0] array [0:127];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -772,6 +789,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -962,6 +981,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [287:0] array [0:127];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -970,6 +1006,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1217,6 +1255,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [287:0] array [0:127];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1224,6 +1279,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v
index f8b0c81..7ceb2d0 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X32_GL_M2_D2.v
@@ -749,6 +749,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [31:0] array [0:127];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -812,6 +829,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1019,6 +1038,23 @@ endtask
 // Ramgen function for reading the arrays
 function [31:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [31:0] array [0:127];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [31:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1030,6 +1066,8 @@ reg [31:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1277,6 +1315,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [31:0] array [0:127];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1284,6 +1339,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v
index 3c72b16..d70af20 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X60_GL_M1_D2.v
@@ -712,6 +712,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 60;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [59:0] array [0:127];
+    reg [59:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -772,6 +789,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -962,6 +981,23 @@ endtask
 // Ramgen function for reading the arrays
 function [59:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 60;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [59:0] array [0:127];
+    reg [59:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [59:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -970,6 +1006,8 @@ reg [59:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1217,6 +1255,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 60;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [59:0] array [0:127];
+    reg [59:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1224,6 +1279,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
index a0a6e43..7a8d4a3 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
@@ -718,6 +718,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [63:0] array [0:127];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -778,6 +795,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -968,6 +987,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [63:0] array [0:127];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -976,6 +1012,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1223,6 +1261,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [63:0] array [0:127];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1230,6 +1285,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
index 6a1e771..ccd5519 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
@@ -781,6 +781,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [143:0] array [0:159];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -844,6 +861,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1051,6 +1070,23 @@ endtask
 // Ramgen function for reading the arrays
 function [143:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [143:0] array [0:159];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [143:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1062,6 +1098,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1309,6 +1347,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [143:0] array [0:159];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1316,6 +1371,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
index 7f9d38b..ee7d742 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
@@ -781,6 +781,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [15:0] array [0:159];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -844,6 +861,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1051,6 +1070,23 @@ endtask
 // Ramgen function for reading the arrays
 function [15:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [15:0] array [0:159];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [15:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1062,6 +1098,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1309,6 +1347,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [15:0] array [0:159];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1316,6 +1371,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
index ac66240..57b5d32 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
@@ -787,6 +787,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 65;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [64:0] array [0:159];
+    reg [64:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -850,6 +867,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1057,6 +1076,23 @@ endtask
 // Ramgen function for reading the arrays
 function [64:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 65;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [64:0] array [0:159];
+    reg [64:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [64:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1068,6 +1104,8 @@ reg [64:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1315,6 +1353,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 65;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [64:0] array [0:159];
+    reg [64:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1322,6 +1377,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
index 473cb85..870587d 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
@@ -781,6 +781,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [81:0] array [0:159];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -844,6 +861,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1051,6 +1070,23 @@ endtask
 // Ramgen function for reading the arrays
 function [81:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [81:0] array [0:159];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [81:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1062,6 +1098,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1309,6 +1347,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [81:0] array [0:159];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1316,6 +1371,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v
index f4d8aff..a0cf8c6 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_160X93_GL_M2_D2.v
@@ -781,6 +781,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 93;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [92:0] array [0:159];
+    reg [92:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -844,6 +861,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1051,6 +1070,23 @@ endtask
 // Ramgen function for reading the arrays
 function [92:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 93;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [92:0] array [0:159];
+    reg [92:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [92:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1062,6 +1098,8 @@ reg [92:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1309,6 +1347,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 93;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [92:0] array [0:159];
+    reg [92:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1316,6 +1371,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
index 12b2897..331cb4d 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
@@ -875,6 +875,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [143:0] array [0:247];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -938,6 +955,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1144,6 +1163,23 @@ endtask
 // Ramgen function for reading the arrays
 function [143:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [143:0] array [0:247];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [143:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1155,6 +1191,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1402,6 +1440,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [143:0] array [0:247];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1409,6 +1464,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
index dea1a7c..18a0adb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
@@ -875,6 +875,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [81:0] array [0:247];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -938,6 +955,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1144,6 +1163,23 @@ endtask
 // Ramgen function for reading the arrays
 function [81:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [81:0] array [0:247];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [81:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1155,6 +1191,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1402,6 +1440,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [81:0] array [0:247];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1409,6 +1464,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v
index 786dccd..e04e231 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X112_GL_M2_D2.v
@@ -877,6 +877,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 112;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [111:0] array [0:255];
+    reg [111:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -940,6 +957,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1145,6 +1164,23 @@ endtask
 // Ramgen function for reading the arrays
 function [111:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 112;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [111:0] array [0:255];
+    reg [111:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [111:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1156,6 +1192,8 @@ reg [111:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1403,6 +1441,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 112;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [111:0] array [0:255];
+    reg [111:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1410,6 +1465,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v
index a9c7860..0c1f1bb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X113_GL_M2_D2.v
@@ -877,6 +877,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 113;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [112:0] array [0:255];
+    reg [112:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -940,6 +957,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1145,6 +1164,23 @@ endtask
 // Ramgen function for reading the arrays
 function [112:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 113;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [112:0] array [0:255];
+    reg [112:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [112:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1156,6 +1192,8 @@ reg [112:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1403,6 +1441,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 113;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [112:0] array [0:255];
+    reg [112:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1410,6 +1465,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
index b8e28c9..f72a1cc 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
@@ -941,6 +941,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [10:0] array [0:255];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1008,6 +1025,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1243,6 +1262,23 @@ endtask
 // Ramgen function for reading the arrays
 function [10:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [10:0] array [0:255];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [10:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1258,6 +1294,8 @@ reg [10:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1505,6 +1543,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [10:0] array [0:255];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1512,6 +1567,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v
index a68ef8f..f80e936 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X128_GL_M2_D2.v
@@ -877,6 +877,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [127:0] array [0:255];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -940,6 +957,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1145,6 +1164,23 @@ endtask
 // Ramgen function for reading the arrays
 function [127:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [127:0] array [0:255];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [127:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1156,6 +1192,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1403,6 +1441,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [127:0] array [0:255];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1410,6 +1465,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
index 0eb14b4..b2be684 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
@@ -877,6 +877,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [143:0] array [0:255];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -940,6 +957,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1145,6 +1164,23 @@ endtask
 // Ramgen function for reading the arrays
 function [143:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [143:0] array [0:255];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [143:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1156,6 +1192,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1403,6 +1441,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [143:0] array [0:255];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1410,6 +1465,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v
index 49c58d2..406e215 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X14_GL_M4_D2.v
@@ -935,6 +935,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [13:0] array [0:255];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1002,6 +1019,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1237,6 +1256,23 @@ endtask
 // Ramgen function for reading the arrays
 function [13:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [13:0] array [0:255];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [13:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1252,6 +1288,8 @@ reg [13:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1499,6 +1537,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [13:0] array [0:255];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1506,6 +1561,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v
index 4fc1554..97da69c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X16_GL_M2_D2.v
@@ -877,6 +877,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [15:0] array [0:255];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -940,6 +957,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1145,6 +1164,23 @@ endtask
 // Ramgen function for reading the arrays
 function [15:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [15:0] array [0:255];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [15:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1156,6 +1192,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1403,6 +1441,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [15:0] array [0:255];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1410,6 +1465,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
index aa028e3..a3d6e0e 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
@@ -883,6 +883,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [63:0] array [0:255];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -946,6 +963,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1151,6 +1170,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [63:0] array [0:255];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1162,6 +1198,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1409,6 +1447,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [63:0] array [0:255];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1416,6 +1471,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
index fd5f968..21539fb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
@@ -883,6 +883,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [79:0] array [0:255];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -946,6 +963,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1151,6 +1170,23 @@ endtask
 // Ramgen function for reading the arrays
 function [79:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [79:0] array [0:255];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [79:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1162,6 +1198,8 @@ reg [79:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1409,6 +1447,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [79:0] array [0:255];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1416,6 +1471,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v
index 4a75907..05d9418 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X128_GL_M1_D2.v
@@ -616,6 +616,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [127:0] array [0:31];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -676,6 +693,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -868,6 +887,23 @@ endtask
 // Ramgen function for reading the arrays
 function [127:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [127:0] array [0:31];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [127:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -876,6 +912,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1123,6 +1161,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [127:0] array [0:31];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1130,6 +1185,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v
index 7cc4359..bce531f 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X130_GL_M1_D2.v
@@ -616,6 +616,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [129:0] array [0:31];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -676,6 +693,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -868,6 +887,23 @@ endtask
 // Ramgen function for reading the arrays
 function [129:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [129:0] array [0:31];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [129:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -876,6 +912,8 @@ reg [129:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1123,6 +1161,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [129:0] array [0:31];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1130,6 +1185,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
index 76bc1af..74f05d8 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 192;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [191:0] array [0:31];
+    reg [191:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [191:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 192;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [191:0] array [0:31];
+    reg [191:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [191:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [191:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 192;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [191:0] array [0:31];
+    reg [191:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
index 8dc6415..6c802f9 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [223:0] array [0:31];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [223:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [223:0] array [0:31];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [223:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [223:0] array [0:31];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
index 6c8c109..20e1c0b 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
@@ -616,6 +616,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [255:0] array [0:31];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -676,6 +693,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -868,6 +887,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [255:0] array [0:31];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -876,6 +912,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1123,6 +1161,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [255:0] array [0:31];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1130,6 +1185,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
index ee4ea9c..b558519 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [271:0] array [0:31];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [271:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [271:0] array [0:31];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [271:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [271:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [271:0] array [0:31];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
index 8b8180a..9796db3 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [287:0] array [0:31];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [287:0] array [0:31];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [287:0] array [0:31];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
index 708bfdb..e5ae9d7 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 40;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [39:0] array [0:511];
+    reg [39:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [39:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 40;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [39:0] array [0:511];
+    reg [39:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [39:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [39:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 40;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [39:0] array [0:511];
+    reg [39:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v
index d80a1e5..3f56bef 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X56_GL_M4_D2.v
@@ -1191,6 +1191,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 56;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [55:0] array [0:511];
+    reg [55:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1258,6 +1275,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1491,6 +1510,23 @@ endtask
 // Ramgen function for reading the arrays
 function [55:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 56;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [55:0] array [0:511];
+    reg [55:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [55:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1506,6 +1542,8 @@ reg [55:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1753,6 +1791,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 56;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [55:0] array [0:511];
+    reg [55:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1760,6 +1815,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
index 49195cc..ea29838 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [63:0] array [0:511];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [63:0] array [0:511];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [63:0] array [0:511];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
index 37c360d..3e97ff5 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [71:0] array [0:511];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [71:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [71:0] array [0:511];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [71:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [71:0] array [0:511];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
index f30712a..fefca56 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [7:0] array [0:511];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [7:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [7:0] array [0:511];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [7:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [7:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [7:0] array [0:511];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
index 20e5d3d..60815d7 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
@@ -644,6 +644,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 168;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [167:0] array [0:59];
+    reg [167:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -704,6 +721,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -897,6 +916,23 @@ endtask
 // Ramgen function for reading the arrays
 function [167:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 168;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [167:0] array [0:59];
+    reg [167:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [167:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -905,6 +941,8 @@ reg [167:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1152,6 +1190,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 168;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [167:0] array [0:59];
+    reg [167:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1159,6 +1214,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v
index ad1ace0..813cc95 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_60X42_GL_M1_D2.v
@@ -644,6 +644,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 42;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [41:0] array [0:59];
+    reg [41:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -704,6 +721,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -897,6 +916,23 @@ endtask
 // Ramgen function for reading the arrays
 function [41:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 42;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [41:0] array [0:59];
+    reg [41:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [41:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -905,6 +941,8 @@ reg [41:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1152,6 +1190,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 42;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [41:0] array [0:59];
+    reg [41:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1159,6 +1214,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v
index be09abc..d49cfa3 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_60X84_GL_M1_D2.v
@@ -644,6 +644,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 84;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [83:0] array [0:59];
+    reg [83:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -704,6 +721,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -897,6 +916,23 @@ endtask
 // Ramgen function for reading the arrays
 function [83:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 84;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [83:0] array [0:59];
+    reg [83:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [83:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -905,6 +941,8 @@ reg [83:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1152,6 +1190,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 84;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [83:0] array [0:59];
+    reg [83:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1159,6 +1214,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
index c06fdf8..9afcd83 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 116;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [115:0] array [0:63];
+    reg [115:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [115:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 116;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [115:0] array [0:63];
+    reg [115:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [115:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [115:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 116;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [115:0] array [0:63];
+    reg [115:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v
index bf161e4..d0c443c 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X128_GL_M1_D2.v
@@ -648,6 +648,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [127:0] array [0:63];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -708,6 +725,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -900,6 +919,23 @@ endtask
 // Ramgen function for reading the arrays
 function [127:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [127:0] array [0:63];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [127:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -908,6 +944,8 @@ reg [127:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1155,6 +1193,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 128;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [127:0] array [0:63];
+    reg [127:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1162,6 +1217,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v
index bb21a6f..da1ae4a 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X130_GL_M1_D2.v
@@ -648,6 +648,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [129:0] array [0:63];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -708,6 +725,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -900,6 +919,23 @@ endtask
 // Ramgen function for reading the arrays
 function [129:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [129:0] array [0:63];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [129:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -908,6 +944,8 @@ reg [129:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1155,6 +1193,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 130;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [129:0] array [0:63];
+    reg [129:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1162,6 +1217,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
index 8aae9be..452f66b 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 160;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [159:0] array [0:63];
+    reg [159:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [159:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 160;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [159:0] array [0:63];
+    reg [159:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [159:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [159:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 160;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [159:0] array [0:63];
+    reg [159:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
index cf976c1..2979e77 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [223:0] array [0:63];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [223:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [223:0] array [0:63];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [223:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [223:0] array [0:63];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
index 80cec6d..d195e5a 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [225:0] array [0:63];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [225:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [225:0] array [0:63];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [225:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [225:0] array [0:63];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v
index 983fec6..72c3cb0 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X256_GL_M1_D2.v
@@ -648,6 +648,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [255:0] array [0:63];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -708,6 +725,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -900,6 +919,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [255:0] array [0:63];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -908,6 +944,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1155,6 +1193,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [255:0] array [0:63];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1162,6 +1217,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
index 1aa844d..3188334 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [287:0] array [0:63];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [287:0] array [0:63];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [287:0] array [0:63];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
index 905a763..e0f3357 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [63:0] array [0:63];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [63:0] array [0:63];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [63:0] array [0:63];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
index 8f87fb5..4b60173 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [65:0] array [0:63];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [65:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [65:0] array [0:63];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [65:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [65:0] array [0:63];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
index ccd0ab8..278d86a 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
@@ -701,6 +701,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [15:0] array [0:79];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -764,6 +781,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -973,6 +992,23 @@ endtask
 // Ramgen function for reading the arrays
 function [15:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [15:0] array [0:79];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [15:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -984,6 +1020,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1231,6 +1269,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [15:0] array [0:79];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1238,6 +1293,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v
index cc67ddc..718c3bb 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X17_GL_M2_D2.v
@@ -701,6 +701,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 17;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [16:0] array [0:79];
+    reg [16:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -764,6 +781,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -973,6 +992,23 @@ endtask
 // Ramgen function for reading the arrays
 function [16:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 17;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [16:0] array [0:79];
+    reg [16:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [16:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -984,6 +1020,8 @@ reg [16:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1231,6 +1269,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 17;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [16:0] array [0:79];
+    reg [16:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1238,6 +1293,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
index 23fb54e..67d6073 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
@@ -664,6 +664,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [225:0] array [0:79];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -724,6 +741,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -916,6 +935,23 @@ endtask
 // Ramgen function for reading the arrays
 function [225:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [225:0] array [0:79];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [225:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -924,6 +960,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1171,6 +1209,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [225:0] array [0:79];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1178,6 +1233,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v
index b6fe412..c76f909 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X238_GL_M1_D2.v
@@ -664,6 +664,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 238;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [237:0] array [0:79];
+    reg [237:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -724,6 +741,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -916,6 +935,23 @@ endtask
 // Ramgen function for reading the arrays
 function [237:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 238;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [237:0] array [0:79];
+    reg [237:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [237:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -924,6 +960,8 @@ reg [237:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1171,6 +1209,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 238;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [237:0] array [0:79];
+    reg [237:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1178,6 +1233,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
index f72ea46..29b57fa 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [255:0] array [0:79];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [255:0] array [0:79];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [255:0] array [0:79];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
index fb90f47..140bd94 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
@@ -664,6 +664,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [287:0] array [0:79];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -724,6 +741,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -916,6 +935,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [287:0] array [0:79];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -924,6 +960,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1171,6 +1209,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [287:0] array [0:79];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1178,6 +1233,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v
index 2ce5420..5a9d9f2 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X36_GL_M1_D2.v
@@ -664,6 +664,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 36;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [35:0] array [0:79];
+    reg [35:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -724,6 +741,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -916,6 +935,23 @@ endtask
 // Ramgen function for reading the arrays
 function [35:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 36;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [35:0] array [0:79];
+    reg [35:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [35:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -924,6 +960,8 @@ reg [35:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1171,6 +1209,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 36;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [35:0] array [0:79];
+    reg [35:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1178,6 +1233,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
index e11f7be..41a260a 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [65:0] array [0:79];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [65:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [65:0] array [0:79];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [65:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [65:0] array [0:79];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v b/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
index 1b22c61..803cfa2 100644
--- a/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
+++ b/vsrc/large/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
@@ -664,6 +664,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [71:0] array [0:79];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -724,6 +741,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -916,6 +935,23 @@ endtask
 // Ramgen function for reading the arrays
 function [71:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [71:0] array [0:79];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [71:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -924,6 +960,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1171,6 +1209,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [71:0] array [0:79];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1178,6 +1233,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
index 34226ea..4e10af9 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_128X11_GL_M2_E2.v
@@ -485,6 +485,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [10:0] array [0:127];
+    reg [10:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -557,6 +574,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -574,6 +593,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [10:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [10:0] array [0:127];
+    reg [10:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +689,8 @@ output [10:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -945,6 +983,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [10:0] array [0:127];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -952,6 +1007,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
index 4998aff..49413ae 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_128X6_GL_M2_E2.v
@@ -485,6 +485,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 6;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [5:0] array [0:127];
+    reg [5:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -557,6 +574,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -574,6 +593,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [5:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 6;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [5:0] array [0:127];
+    reg [5:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -653,6 +689,8 @@ output [5:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -945,6 +983,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 6;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [5:0] array [0:127];
+    reg [5:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -952,6 +1007,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
index 999f860..fd02a48 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_16X256_GL_M1_E2.v
@@ -340,6 +340,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [255:0] array [0:15];
+    reg [255:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -412,6 +429,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -429,6 +448,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [255:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [255:0] array [0:15];
+    reg [255:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +544,8 @@ output [255:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -788,6 +826,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [255:0] array [0:15];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -795,6 +850,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
index 08605dd..ad1d344 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_16X272_GL_M1_E2.v
@@ -340,6 +340,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [271:0] array [0:15];
+    reg [271:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -412,6 +429,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -429,6 +448,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [271:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [271:0] array [0:15];
+    reg [271:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +544,8 @@ output [271:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -788,6 +826,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [271:0] array [0:15];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -795,6 +850,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
index 5548288..d778134 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_16X64_GL_M1_E2.v
@@ -340,6 +340,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [63:0] array [0:15];
+    reg [63:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -412,6 +429,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -429,6 +448,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [63:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [63:0] array [0:15];
+    reg [63:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -508,6 +544,8 @@ output [63:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -788,6 +826,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 16;
+    (* ram_style = "block" *) reg [63:0] array [0:15];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -795,6 +850,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
index f7d8b75..5ad2416 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_20X288_GL_M1_E2.v
@@ -352,6 +352,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [287:0] array [0:19];
+    reg [287:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -424,6 +441,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -441,6 +460,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [287:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [287:0] array [0:19];
+    reg [287:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +556,8 @@ output [287:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -800,6 +838,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [287:0] array [0:19];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -807,6 +862,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
index ddbcbaf..6abd6d8 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_20X80_GL_M1_E2.v
@@ -352,6 +352,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [79:0] array [0:19];
+    reg [79:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -424,6 +441,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -441,6 +460,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [79:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [79:0] array [0:19];
+    reg [79:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -520,6 +556,8 @@ output [79:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -800,6 +838,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 20;
+    (* ram_style = "block" *) reg [79:0] array [0:19];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -807,6 +862,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
index bc8af2e..4eed766 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_256X4_GL_M2_E2.v
@@ -613,6 +613,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 4;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [3:0] array [0:255];
+    reg [3:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -685,6 +702,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -702,6 +721,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [3:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 4;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [3:0] array [0:255];
+    reg [3:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +817,8 @@ output [3:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -1073,6 +1111,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 4;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [3:0] array [0:255];
+    reg [3:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1080,6 +1135,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
index 0beca22..e9b3188 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_256X7_GL_M2_E2.v
@@ -613,6 +613,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 7;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [6:0] array [0:255];
+    reg [6:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -685,6 +702,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -702,6 +721,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [6:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 7;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [6:0] array [0:255];
+    reg [6:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +817,8 @@ output [6:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -1073,6 +1111,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 7;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [6:0] array [0:255];
+    reg [6:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1080,6 +1135,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
index cd77293..a452dc6 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_256X8_GL_M2_E2.v
@@ -613,6 +613,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [7:0] array [0:255];
+    reg [7:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -685,6 +702,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -702,6 +721,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [7:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [7:0] array [0:255];
+    reg [7:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -781,6 +817,8 @@ output [7:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -1073,6 +1111,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [7:0] array [0:255];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1080,6 +1135,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
index 42a3813..9600aeb 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_32X16_GL_M1_E2.v
@@ -356,6 +356,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [15:0] array [0:31];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -428,6 +445,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -445,6 +464,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [15:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [15:0] array [0:31];
+    reg [15:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +560,8 @@ output [15:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -804,6 +842,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [15:0] array [0:31];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -811,6 +866,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
index 1969fbc..b3bc03a 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_32X32_GL_M1_E2.v
@@ -356,6 +356,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [31:0] array [0:31];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -428,6 +445,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -445,6 +464,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [31:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [31:0] array [0:31];
+    reg [31:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -524,6 +560,8 @@ output [31:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -804,6 +842,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 32;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [31:0] array [0:31];
+    reg [31:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -811,6 +866,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
index d85ccbb..7cf3af5 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_60X22_GL_M1_E2.v
@@ -392,6 +392,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 22;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [21:0] array [0:59];
+    reg [21:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -464,6 +481,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -481,6 +500,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [21:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 22;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [21:0] array [0:59];
+    reg [21:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -560,6 +596,8 @@ output [21:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -840,6 +878,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 22;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [21:0] array [0:59];
+    reg [21:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -847,6 +902,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
index b54f48b..b81feb0 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_64X10_GL_M2_E2.v
@@ -421,6 +421,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 10;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [9:0] array [0:63];
+    reg [9:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -493,6 +510,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -510,6 +529,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [9:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 10;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [9:0] array [0:63];
+    reg [9:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +625,8 @@ output [9:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -881,6 +919,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 10;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [9:0] array [0:63];
+    reg [9:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -888,6 +943,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
index e85c431..24bc639 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_64X18_GL_M2_E2.v
@@ -421,6 +421,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [17:0] array [0:63];
+    reg [17:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -493,6 +510,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -510,6 +529,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [17:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [17:0] array [0:63];
+    reg [17:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -589,6 +625,8 @@ output [17:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -881,6 +919,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [17:0] array [0:63];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -888,6 +943,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
index b236466..426ef8d 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_80X14_GL_M2_E2.v
@@ -445,6 +445,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [13:0] array [0:79];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -517,6 +534,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -534,6 +553,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [13:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [13:0] array [0:79];
+    reg [13:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +649,8 @@ output [13:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -905,6 +943,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 14;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [13:0] array [0:79];
+    reg [13:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -912,6 +967,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
index 6cb9768..304a929 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_80X15_GL_M2_E2.v
@@ -445,6 +445,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 15;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [14:0] array [0:79];
+    reg [14:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -517,6 +534,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -534,6 +553,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [14:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 15;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [14:0] array [0:79];
+    reg [14:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +649,8 @@ output [14:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -905,6 +943,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 15;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [14:0] array [0:79];
+    reg [14:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -912,6 +967,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v b/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
index 7ba9e4b..734c9c0 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_80X9_GL_M2_E2.v
@@ -445,6 +445,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 9;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [8:0] array [0:79];
+    reg [8:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -517,6 +534,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -534,6 +553,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [8:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 9;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [8:0] array [0:79];
+    reg [8:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -613,6 +649,8 @@ output [8:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -905,6 +943,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 9;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [8:0] array [0:79];
+    reg [8:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -912,6 +967,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v b/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
index 58cbe53..e620851 100644
--- a/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
+++ b/vsrc/small/vmod/rams/model/RAMDP_8X66_GL_M1_E2.v
@@ -327,6 +327,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [65:0] array [0:7];
+    reg [65:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
   integer r;
   integer c;
     ITOP.io.clear_bit_fault_stuck_1(r,c);
@@ -399,6 +416,8 @@ endtask
 `endif //def EMULATION
 //VCS coverage on
 `endif //ndef RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
@@ -416,6 +435,23 @@ input [1:0] SVOP;
 input [7:0] SLEEP_EN;
 input clobber_flops, clobber_array, clobber_x;
 output [65:0] RD;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [65:0] array [0:7];
+    reg [65:0] r_data;
+
+    always @(posedge CLK_W) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLK_R) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 //State point clobering signals:
     wire output_valid = ~(clobber_x | (|SLEEP_EN));
     wire clamp_o = SLEEP_EN[7] | RET_EN;
@@ -495,6 +531,8 @@ output [65:0] RD;
   .r0_dout(dout),
         .clamp_o(clamp_o)
  );
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
@@ -775,6 +813,23 @@ endtask
 task clear_bit_fault_stuck_1;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 8;
+    (* ram_style = "block" *) reg [65:0] array [0:7];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -782,6 +837,8 @@ endtask
 //VCS coverage on
 `endif //def FAULT_INJECTION
 `endif //ndef SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif //ndef EMULATION
 `endif //ndef RAM_INTERFACE
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
index 3cdd88c..2663679 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_128X18_GL_M2_D2.v
@@ -755,6 +755,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [17:0] array [0:127];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -818,6 +835,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1025,6 +1044,23 @@ endtask
 // Ramgen function for reading the arrays
 function [17:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [17:0] array [0:127];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [17:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1036,6 +1072,8 @@ reg [17:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1283,6 +1321,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 18;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [17:0] array [0:127];
+    reg [17:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1290,6 +1345,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
index 162e3b8..67a6ae8 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_128X256_GL_M1_D2.v
@@ -718,6 +718,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [255:0] array [0:127];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -778,6 +795,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -968,6 +987,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [255:0] array [0:127];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -976,6 +1012,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1223,6 +1261,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [255:0] array [0:127];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1230,6 +1285,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
index a0a6e43..7a8d4a3 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_128X64_GL_M1_D2.v
@@ -718,6 +718,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [63:0] array [0:127];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -778,6 +795,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -968,6 +987,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [63:0] array [0:127];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -976,6 +1012,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1223,6 +1261,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 128;
+    (* ram_style = "block" *) reg [63:0] array [0:127];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1230,6 +1285,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
index f691b6b..d07cb6c 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X144_GL_M2_D2.v
@@ -787,6 +787,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [143:0] array [0:159];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -850,6 +867,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1057,6 +1076,23 @@ endtask
 // Ramgen function for reading the arrays
 function [143:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [143:0] array [0:159];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [143:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1068,6 +1104,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1315,6 +1353,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [143:0] array [0:159];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1322,6 +1377,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
index 3ba4c18..57e6eb1 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X16_GL_M2_D2.v
@@ -787,6 +787,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [15:0] array [0:159];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -850,6 +867,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1057,6 +1076,23 @@ endtask
 // Ramgen function for reading the arrays
 function [15:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [15:0] array [0:159];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [15:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1068,6 +1104,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1315,6 +1353,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [15:0] array [0:159];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1322,6 +1377,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
index ac66240..57b5d32 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X65_GL_M2_D2.v
@@ -787,6 +787,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 65;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [64:0] array [0:159];
+    reg [64:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -850,6 +867,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1057,6 +1076,23 @@ endtask
 // Ramgen function for reading the arrays
 function [64:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 65;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [64:0] array [0:159];
+    reg [64:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [64:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1068,6 +1104,8 @@ reg [64:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1315,6 +1353,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 65;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [64:0] array [0:159];
+    reg [64:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1322,6 +1377,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
index 9bafc68..2e76b3d 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_160X82_GL_M2_D2.v
@@ -787,6 +787,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [81:0] array [0:159];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -850,6 +867,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1057,6 +1076,23 @@ endtask
 // Ramgen function for reading the arrays
 function [81:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [81:0] array [0:159];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [81:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1068,6 +1104,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1315,6 +1353,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 160;
+    (* ram_style = "block" *) reg [81:0] array [0:159];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1322,6 +1377,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
index 12b2897..331cb4d 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_248X144_GL_M2_D2.v
@@ -875,6 +875,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [143:0] array [0:247];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -938,6 +955,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1144,6 +1163,23 @@ endtask
 // Ramgen function for reading the arrays
 function [143:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [143:0] array [0:247];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [143:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1155,6 +1191,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1402,6 +1440,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [143:0] array [0:247];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1409,6 +1464,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
index dea1a7c..18a0adb 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_248X82_GL_M2_D2.v
@@ -875,6 +875,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [81:0] array [0:247];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -938,6 +955,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1144,6 +1163,23 @@ endtask
 // Ramgen function for reading the arrays
 function [81:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [81:0] array [0:247];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [81:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1155,6 +1191,8 @@ reg [81:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1402,6 +1440,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 82;
+    localparam words = 248;
+    (* ram_style = "block" *) reg [81:0] array [0:247];
+    reg [81:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1409,6 +1464,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
index b8e28c9..f72a1cc 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X11_GL_M4_D2.v
@@ -941,6 +941,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [10:0] array [0:255];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1008,6 +1025,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1243,6 +1262,23 @@ endtask
 // Ramgen function for reading the arrays
 function [10:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [10:0] array [0:255];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [10:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1258,6 +1294,8 @@ reg [10:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1505,6 +1543,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 11;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [10:0] array [0:255];
+    reg [10:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1512,6 +1567,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
index 050d585..6c67d12 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X144_GL_M2_D2.v
@@ -883,6 +883,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [143:0] array [0:255];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -946,6 +963,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1151,6 +1170,23 @@ endtask
 // Ramgen function for reading the arrays
 function [143:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [143:0] array [0:255];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [143:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1162,6 +1198,8 @@ reg [143:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1409,6 +1447,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 144;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [143:0] array [0:255];
+    reg [143:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1416,6 +1471,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
index aa028e3..a3d6e0e 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X64_GL_M2_D2.v
@@ -883,6 +883,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [63:0] array [0:255];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -946,6 +963,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1151,6 +1170,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [63:0] array [0:255];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1162,6 +1198,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1409,6 +1447,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [63:0] array [0:255];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1416,6 +1471,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
index fd5f968..21539fb 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_256X80_GL_M2_D2.v
@@ -883,6 +883,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [79:0] array [0:255];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -946,6 +963,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1151,6 +1170,23 @@ endtask
 // Ramgen function for reading the arrays
 function [79:0] mem_read_bank;
 input [7:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [79:0] array [0:255];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [79:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -1162,6 +1198,8 @@ reg [79:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1409,6 +1447,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 80;
+    localparam words = 256;
+    (* ram_style = "block" *) reg [79:0] array [0:255];
+    reg [79:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1416,6 +1471,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
index 76bc1af..74f05d8 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X192_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 192;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [191:0] array [0:31];
+    reg [191:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [191:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 192;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [191:0] array [0:31];
+    reg [191:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [191:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [191:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 192;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [191:0] array [0:31];
+    reg [191:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
index 8dc6415..6c802f9 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X224_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [223:0] array [0:31];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [223:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [223:0] array [0:31];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [223:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [223:0] array [0:31];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
index e4140d1..1e3088f 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X256_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [255:0] array [0:31];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [255:0] array [0:31];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [255:0] array [0:31];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
index ee4ea9c..b558519 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X272_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [271:0] array [0:31];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [271:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [271:0] array [0:31];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [271:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [271:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 272;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [271:0] array [0:31];
+    reg [271:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
index 8b8180a..9796db3 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_32X288_GL_M1_D2.v
@@ -622,6 +622,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [287:0] array [0:31];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -682,6 +699,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -874,6 +893,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [4:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [287:0] array [0:31];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -882,6 +918,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1129,6 +1167,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 32;
+    (* ram_style = "block" *) reg [287:0] array [0:31];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1136,6 +1191,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
index 708bfdb..e5ae9d7 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X40_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 40;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [39:0] array [0:511];
+    reg [39:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [39:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 40;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [39:0] array [0:511];
+    reg [39:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [39:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [39:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 40;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [39:0] array [0:511];
+    reg [39:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
index 49195cc..ea29838 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X64_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [63:0] array [0:511];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [63:0] array [0:511];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [63:0] array [0:511];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
index 37c360d..3e97ff5 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X72_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [71:0] array [0:511];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [71:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [71:0] array [0:511];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [71:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [71:0] array [0:511];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
index f30712a..fefca56 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_512X8_GL_M4_D2.v
@@ -1197,6 +1197,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [7:0] array [0:511];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 4) == 0)
@@ -1264,6 +1281,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -1497,6 +1516,23 @@ endtask
 // Ramgen function for reading the arrays
 function [7:0] mem_read_bank;
 input [8:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [7:0] array [0:511];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [7:0] memout;
   begin
     if (addr[1:0] == 2'b00)
@@ -1512,6 +1548,8 @@ reg [7:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1759,6 +1797,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 8;
+    localparam words = 512;
+    (* ram_style = "block" *) reg [7:0] array [0:511];
+    reg [7:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1766,6 +1821,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
index bcac485..38087b4 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_60X168_GL_M1_D2.v
@@ -650,6 +650,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 168;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [167:0] array [0:59];
+    reg [167:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -710,6 +727,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -903,6 +922,23 @@ endtask
 // Ramgen function for reading the arrays
 function [167:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 168;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [167:0] array [0:59];
+    reg [167:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [167:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -911,6 +947,8 @@ reg [167:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1158,6 +1196,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 168;
+    localparam words = 60;
+    (* ram_style = "block" *) reg [167:0] array [0:59];
+    reg [167:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1165,6 +1220,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
index c06fdf8..9afcd83 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X116_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 116;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [115:0] array [0:63];
+    reg [115:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [115:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 116;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [115:0] array [0:63];
+    reg [115:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [115:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [115:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 116;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [115:0] array [0:63];
+    reg [115:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
index 8aae9be..452f66b 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X160_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 160;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [159:0] array [0:63];
+    reg [159:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [159:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 160;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [159:0] array [0:63];
+    reg [159:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [159:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [159:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 160;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [159:0] array [0:63];
+    reg [159:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
index cf976c1..2979e77 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X224_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [223:0] array [0:63];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [223:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [223:0] array [0:63];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [223:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [223:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 224;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [223:0] array [0:63];
+    reg [223:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
index 80cec6d..d195e5a 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X226_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [225:0] array [0:63];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [225:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [225:0] array [0:63];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [225:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [225:0] array [0:63];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
index 1aa844d..3188334 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X288_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [287:0] array [0:63];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [287:0] array [0:63];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [287:0] array [0:63];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
index 905a763..e0f3357 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X64_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [63:0] array [0:63];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [63:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [63:0] array [0:63];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [63:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [63:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 64;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [63:0] array [0:63];
+    reg [63:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
index 8f87fb5..4b60173 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_64X66_GL_M1_D2.v
@@ -654,6 +654,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [65:0] array [0:63];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -714,6 +731,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -906,6 +925,23 @@ endtask
 // Ramgen function for reading the arrays
 function [65:0] mem_read_bank;
 input [5:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [65:0] array [0:63];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [65:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -914,6 +950,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1161,6 +1199,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 64;
+    (* ram_style = "block" *) reg [65:0] array [0:63];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1168,6 +1223,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
index dbd5e85..e6cb0a2 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X16_GL_M2_D2.v
@@ -707,6 +707,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [15:0] array [0:79];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
   if ( (r % 2) == 0)
@@ -770,6 +787,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -979,6 +998,23 @@ endtask
 // Ramgen function for reading the arrays
 function [15:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [15:0] array [0:79];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [15:0] memout;
   begin
     if (addr[0] == 1'b0)
@@ -990,6 +1026,8 @@ reg [15:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1237,6 +1275,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 16;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [15:0] array [0:79];
+    reg [15:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1244,6 +1299,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
index ed8a3f9..0068e02 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X226_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [225:0] array [0:79];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [225:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [225:0] array [0:79];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [225:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [225:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 226;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [225:0] array [0:79];
+    reg [225:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
index f72ea46..29b57fa 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X256_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [255:0] array [0:79];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [255:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [255:0] array [0:79];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [255:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [255:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 256;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [255:0] array [0:79];
+    reg [255:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
index b3d3336..aac3f14 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X288_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [287:0] array [0:79];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [287:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [287:0] array [0:79];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [287:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [287:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 288;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [287:0] array [0:79];
+    reg [287:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
index e11f7be..41a260a 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X66_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [65:0] array [0:79];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [65:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [65:0] array [0:79];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [65:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [65:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 66;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [65:0] array [0:79];
+    reg [65:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
diff --git a/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v b/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
index b9a5672..783f3ec 100644
--- a/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
+++ b/vsrc/small/vmod/rams/model/RAMPDP_80X72_GL_M1_D2.v
@@ -670,6 +670,23 @@ endtask
 task clear_bit_fault_stuck_1;
 input r;
 input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [71:0] array [0:79];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 integer r;
 integer c;
     ITOP.iow0.clear_bit_fault_stuck_1_subbank(r, c);
@@ -730,6 +747,8 @@ endtask
 `endif // EMULATION
 //VCS coverage on
 `endif // end RAM_INTERFACE
+
+`endif // SYNTHESIS
 endmodule
 `endcelldefine
 `ifndef RAM_INTERFACE
@@ -922,6 +941,23 @@ endtask
 // Ramgen function for reading the arrays
 function [71:0] mem_read_bank;
 input [6:0] addr;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [71:0] array [0:79];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WE) array[WA] <= WD;
+    end
+
+    always @(posedge CLOCK) begin
+        if (RE) r_data <= array[RA];
+    end
+    assign RD = r_data;
+
+`else
+
 reg [71:0] memout;
   begin
     memout = iow0.mem_read_raw_subbank(addr);
@@ -930,6 +966,8 @@ reg [71:0] memout;
 endfunction
 //VCS coverage on
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
@@ -1177,6 +1215,23 @@ endtask
 task clear_bit_fault_stuck_1_subbank;
   input r;
   input c;
+`ifdef SYNTHESIS
+    localparam bits = 72;
+    localparam words = 80;
+    (* ram_style = "block" *) reg [71:0] array [0:79];
+    reg [71:0] r_data;
+
+    always @(posedge CLOCK) begin
+        if (WRITE_EN) array[WADR] <= WDATA;
+    end
+
+    always @(posedge CLOCK) begin
+        r_data <= array[RADR];
+    end
+    assign RDATA = r_data;
+
+`else
+
   integer r;
   integer c;
   bit_fault_stuck_1[r][c] = 0;
@@ -1184,6 +1239,8 @@ endtask
 //VCS coverage on
 `endif //STUCK
 `endif //SYNTHESIS
+
+`endif // SYNTHESIS
 endmodule
 `endif // end EMULATION
 `endif // end RAM_INTERFACE 
